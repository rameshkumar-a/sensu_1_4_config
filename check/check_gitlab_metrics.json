{
  "checks": {
    "prometheus_metrics": {
      "type": "metric",
      "command": "/opt/sensu/embedded/bin/sensu-prometheus-collector -exporter-url PROMETHEUS_HOST_URL",
      "subscribers": ["app_tier"],
      "interval": 10,
      "handlers": [
		      "debug",
	          "influxdb_udp"
	    ]
    }
  }
}